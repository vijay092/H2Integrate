name: driver_config
description: Driver configuration for nuclear + BESS + data center example
general:
  folder_output: nuclear_bess_dc_run

recorder:
  flag: true
  file: results.sql
  overwrite_recorder: false
